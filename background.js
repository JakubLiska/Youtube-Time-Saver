// chrome.runtime.onInstalled.addListener(() => {
  // zapytanie do servera o ustawienie uzytkownika
  // chrome.storage.sync.get(state).then((result) => {
  //   console.log("Value currently is " + result.key);
  // });
// });

// Regexp matches all possible YouTube url combinations.
const youtube = new RegExp(/^((?:https?:)?\/\/)?((?:www|m)\.)?((?:youtube(-nocookie)?\.|youtu.be))/)

